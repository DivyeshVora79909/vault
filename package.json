{
  "name": "vault",
  "version": "3.0.0",
  "private": true,
  "description": "Offline, zero-knowledge file encryption that runs entirely in the browser.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "test:e2e": "node test/e2e.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "playwright": "1.56.0"
  }
}
